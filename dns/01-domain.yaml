
namespace: ${namespace}

stacker_bucket: ''

stacks:

  domain:
    template_path: templates/domain.yaml
    variables:
      HostedZoneName: '${app_domain}.'
      Hostname: '${runway_environment_name}.${app_domain}'
      RegionalCertificateArn: '${certificate_arn}'
      ApiGatewayRestApiId: '${xref ${runway_project_name}-${runway_environment_name}-generator::RestApiId}'
      ApiGatewayStage: '${runway_environment_name}'
