
namespace: ${namespace}

stacker_bucket: ''

stacks:

  domain:
    template_path: templates/domain.yaml
    variables:
      HostedZoneName: '${domain}.'
      Hostname: '${environment}.${domain}'
      RegionalCertificateArn: '${certificate_arn}'
      ApiGatewayRestApiId: '${xref ${namespace}-generator::RestApiId}'
      ApiGatewayStage: '${api_gateway_stage}'